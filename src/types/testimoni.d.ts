export interface CreateTestimoniDTO {
  nama_tester: string;
  testimoni: string;
  is_active: boolean;
}

export interface UpdateTestimoniDTO {
  nama_tester: string;
  testimoni: string;
  is_active: boolean;
}
