export interface CreateJabatanDTO {
  nama_jabatan: string;
  urutan: number;
  is_active: boolean;
}

export interface UpdateJabatanDTO {
  nama_jabatan: string;
  urutan: number;
  is_active: boolean;
}